{
    "build": {
        "builder": "NIXPACKS",
        "buildCommand": "npx prisma generate && npm run build"
    },
    "deploy": {
        "startCommand": "node dist/src/main || node dist/main",
        "restartPolicyType": "ON_FAILURE"
    }
}