{
    "build": {
        "builder": "NIXPACKS",
        "buildCommand": "npx prisma generate && npm run build"
    },
    "deploy": {
        "startCommand": "[ -f dist/src/main.js ] && node dist/src/main.js || node dist/main.js",
        "restartPolicyType": "ON_FAILURE"
    }
}